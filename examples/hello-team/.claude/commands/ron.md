---
description: Scrum Master (alias for /scrum)
alias-of: scrum
---

# /ron → /scrum

This command is an **alias** for **Scrum Master** (`/scrum`).

When invoked, read and follow the full instructions in:

`.claude/commands/scrum.md`

Use the same arguments:

$ARGUMENTS
