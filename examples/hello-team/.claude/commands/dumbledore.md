---
description: Engineering Manager (alias for /manager)
alias-of: manager
---

# /dumbledore → /manager

This command is an **alias** for **Engineering Manager** (`/manager`).

When invoked, read and follow the full instructions in:

`.claude/commands/manager.md`

Use the same arguments:

$ARGUMENTS
