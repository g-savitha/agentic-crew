---
description: Product Owner (alias for /po)
alias-of: po
---

# /draco → /po

This command is an **alias** for **Product Owner** (`/po`).

When invoked, read and follow the full instructions in:

`.claude/commands/po.md`

Use the same arguments:

$ARGUMENTS
