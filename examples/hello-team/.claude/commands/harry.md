---
description: Backend Developer (alias for /backend)
alias-of: backend
---

# /harry → /backend

This command is an **alias** for **Backend Developer** (`/backend`).

When invoked, read and follow the full instructions in:

`.claude/commands/backend.md`

Use the same arguments:

$ARGUMENTS
