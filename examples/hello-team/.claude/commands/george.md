---
description: DevOps Engineer (alias for /devops)
alias-of: devops
---

# /george → /devops

This command is an **alias** for **DevOps Engineer** (`/devops`).

When invoked, read and follow the full instructions in:

`.claude/commands/devops.md`

Use the same arguments:

$ARGUMENTS
