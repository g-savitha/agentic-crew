---
description: Frontend Developer (alias for /frontend)
alias-of: frontend
---

# /ginny → /frontend

This command is an **alias** for **Frontend Developer** (`/frontend`).

When invoked, read and follow the full instructions in:

`.claude/commands/frontend.md`

Use the same arguments:

$ARGUMENTS
