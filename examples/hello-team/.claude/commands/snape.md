---
description: Security Engineer (alias for /security)
alias-of: security
---

# /snape → /security

This command is an **alias** for **Security Engineer** (`/security`).

When invoked, read and follow the full instructions in:

`.claude/commands/security.md`

Use the same arguments:

$ARGUMENTS
