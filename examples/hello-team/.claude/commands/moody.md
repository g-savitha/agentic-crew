---
description: QA Engineer (alias for /qa)
alias-of: qa
---

# /moody → /qa

This command is an **alias** for **QA Engineer** (`/qa`).

When invoked, read and follow the full instructions in:

`.claude/commands/qa.md`

Use the same arguments:

$ARGUMENTS
