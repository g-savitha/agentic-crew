---
description: {{aliasDescription}}
alias-of: {{canonicalFile}}
---

# /{{aliasCommand}} → /{{canonicalFile}}

This command is an **alias** for **{{role}}** (`/{{canonicalFile}}`).

When invoked, read and follow the full instructions in:

`{{commandsRelativePath}}/{{canonicalFile}}.md`

Use the same arguments:

$ARGUMENTS
